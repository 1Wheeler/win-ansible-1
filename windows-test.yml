---
- hosts: all
  vars:
     contents: "{{ lookup('file', 'C:\Windows\Temp\ansiblevars') }}"
  gather_facts: false
  tasks:
    - name: Make a couple Directories
      win_file:
              path: C:\Temp\asdf-123
              state: directory
